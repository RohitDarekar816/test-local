when:
  - event: push
    branch: master

clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      lfs: false

steps:
  - name: show-directory
    image: alpine:latest
    commands:
      - pwd
