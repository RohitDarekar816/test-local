when:
  - event: push
    branch: master

skip_clone: true

steps:
  - name: clone-and-pwd
    commands:
      - echo "Cloning repository manually..."
      - git clone https://gitea.hivefinty.com/admin/test-local.git .
      - echo "Current working directory:"
      - pwd
