when:
  - event: push
    branch: master

skip_clone: true

steps:
  - name: show-directory
    image: sh
    commands:
      - git clone https://admin:admin@gitea.hivefinty.com/admin/cartlog-admin.git
      - cd cartlog-admin
      - ls
      - pwd
